# Pedestal models

- `pedestal.glb` — registry `pedestal/base`
- The Libro de Rosa (registry `pedestal/book`) lives in `models/shared/libro-historia-barrio-abajo.glb`, built by `.vscode/scripts/libro-historia-rosa.py`

Scale: 1 unit = 1m. Origin at pedestal base center / book pivot.
