# Library models

Place Blender exports here:

- `bookshelf.glb` — replaces procedural `Bookshelf` (registry: `library/bookshelf`)
- `scattered-book.glb` — replaces procedural scattered book instance (registry: `library/scattered-book`)

Export: Blender > File > Export > glTF 2.0 (.glb) | Apply Modifiers | +Y Up | Transform applied.
Keep origin at base center for correct placement.
